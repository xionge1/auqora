1. run server
    flask run --debugger

NOTE: if another environment is running or if you run into issues with making it work- kill environment
`rm -rf env`

Then create environemt : `python3 -m venv env`

Activate env:            `source env/bin/activate`

```sh
print('Hello, world!')
x = 1+2
```

install flask : `pip install Flask`

run flask app: `flask run --debugger`


2. In a seperate terminal window: 
use link given `curl 127.0.0.1:5000`

run the python file


